{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env DEBUG=backend:*  PORT=3035 NODE_ENV=dev nodemon ./bin/www",
    "start:build": "cross-env DEBUG=backend:*  PORT=3035 NODE_ENV=prod nodemon ./bin/www",
    "test": "cross-env NODE_ENV=test jest --watchAll --collect-coverage"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "jest": "^27.3.1",
    "mongoose": "^6.0.13",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "supertest": "^6.1.6"
  }
}
